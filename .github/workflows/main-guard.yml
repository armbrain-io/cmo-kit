name: main-guard

on:
  push:
    branches: [main]

permissions:
  checks: read
  contents: read

jobs:
  guard:
    uses: armbrain-io/.github/.github/workflows/main-guard.yml@main
    secrets: inherit
